interface:
  display_name: "Result Finalizer"
  short_description: "Report result, generalize scripts, ingest corpus, release env, cleanup"
  default_prompt: "Use $result-finalizer to finish a test case with result reporting, script generalization, corpus ingestion, environment release, and workspace cleanup."
