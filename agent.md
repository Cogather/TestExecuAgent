# Agent 执行规则

本文件定义编排 Agent 的全局执行约束。

## 脚本执行优先策略

对所有脚本（Python、Shell、Node 等）统一采用以下规则：

1. 在运行脚本前，不要读取脚本文件内容。
2. 先直接通过命令行执行脚本。
3. 只有当脚本执行失败时，才允许读取脚本文件进行排查。
4. 读取范围应最小化，只读取定位问题所需片段。
5. 修复后必须再次通过命令行执行验证。

## 失败判定

以下任一情况视为“执行失败”，可进入读取文件排查阶段：

- 命令退出码非 `0`
- 执行超时
- 运行时异常导致脚本中断
- 缺少必需输入参数或依赖导致无法启动
